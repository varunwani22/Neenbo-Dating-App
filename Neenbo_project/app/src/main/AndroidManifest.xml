<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.neenbo_project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Neenbo_Project">
        <activity android:name=".MyAcount">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PeopleNearby" />
        <activity android:name=".profile" />
        <activity android:name=".TermsOfUse" />
        <activity android:name=".Filter" />
        <activity android:name=".Finish" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>